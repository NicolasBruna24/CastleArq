[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "localai-hub"
version = "0.1.0"
description = "Simple local hardware and AI runtime detection"
requires-python = ">=3.10"

[tool.pytest.ini_options]
pythonpath = ["."]
